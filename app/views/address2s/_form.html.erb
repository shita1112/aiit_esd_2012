<%= form_for(@address2) do |f| %>
  <% if @address2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@address2.errors.count, "error") %> prohibited this address2 from being saved:</h2>

      <ul>
      <% @address2.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
